import { access } from "../Routes";

const Search = {
  coursesList,
}

function coursesList(input) {
  // search courses by input: by name and id
  // return array of course name and id that match input
}



export default Search;